const classesModel = require("../models/Classes");

const handleAddingClasses = async (req, res)=>{
    const {title, category, description, date} = req.body
    try {
        const classData = new classesModel({
            
        })
    } catch (error) {
        
    }
}